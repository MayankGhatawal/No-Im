<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <h1 class="text-6xl uppercase ml-80 font-bold drop-shadow-[0_6px_6px_rgba(0,0,255,0.25)] mb-5">Blog by mayank ghatawal</h1>
        <hr>
        <div class="p-10">
        <form action="/create" method="post">
            <input class="block w-full px-5 py-2 bg-zinc-800 outline-none rounded-lg" type="text" name="title" placeholder="Title..">
            <textarea class="block w-full px-5 mt-2 py-2 bg-zinc-800 resize-none outline-none rounded-lg" name="details" placeholder="Write Your Task...."></textarea>
            <input class="rounded-lg mt-2 p-1 bg-blue-500" type="submit" value="Create task">
        </form>
    </div>
                <div class="tasks flex gap-3 p-10">
                    <% if(files.length > 0){ %>
                        <% files.forEach(function(val){ %>
                            <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-lg">
                                <h1 class="text-xl tracking-tighter"><%= val %></h1>
                                <div class="flex mt-3 justify-between">
                                <a href="/files/<%= val%>" class="text-blue-500 inline-block">Read More</a>
                                <a href="/edit/<%= val%>" class="text-zinc-500 inline-block">Edit Filename</a>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <h3 class="text-zinc-500">no tasks.</h3>
                    <% } %>
                
        </div>
    </div>
    <script src="/javascripts/scripts.js"></script>
</body>
</html>